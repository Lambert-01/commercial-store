    </div>
  </main>

  <!-- Minimal Black & Blue Footer -->
  <footer class="bg-gradient-to-r from-black via-gray-900 to-blue-900 text-slate-300 mt-auto">
    <div class="container mx-auto px-4 py-4">

      <!-- Minimal Footer Content -->
      <div class="flex flex-col md:flex-row items-center justify-between gap-4">

        <!-- Brand Section -->
        <div class="flex items-center gap-3">
          <div class="w-8 h-8 bg-primary rounded-lg flex items-center justify-center text-white font-bold text-sm">
            🇷🇼
          </div>
          <div>
            <h3 class="text-white text-lg font-bold">Ecommerce Rwanda</h3>
          </div>
        </div>

        <!-- Copyright -->
        <div class="text-center">
          <p class="text-slate-400 text-sm">
            &copy; <%= new Date().getFullYear() %> Ecommerce Rwanda. All rights reserved.
          </p>
        </div>

      </div>
    </div>
  </footer>

  <!-- Scripts -->
  <script src="/js/main.js"></script>
  <script src="https://unpkg.com/aos@2.3.4/dist/aos.js"></script>

  <script>
    // Newsletter subscription
    document.querySelector('footer form')?.addEventListener('submit', function(e) {
      e.preventDefault();
      const email = this.querySelector('input[type="email"]').value;

      if (email) {
        const button = this.querySelector('button');
        const originalText = button.textContent;
        button.textContent = '✓ Joined!';
        button.classList.remove('bg-primary');
        button.classList.add('bg-green-600');

        setTimeout(() => {
          button.textContent = originalText;
          button.classList.add('bg-primary');
          button.classList.remove('bg-green-600');
          this.querySelector('input[type="email"]').value = '';
        }, 2000);

        console.log('Newsletter subscription for:', email);
      }
    });

    // Simple footer interactions
    document.addEventListener('DOMContentLoaded', function() {
      // Initialize AOS
      if (typeof AOS !== 'undefined') {
        AOS.init({
          duration: 800,
          easing: 'ease-in-out',
          once: true,
          offset: 100
        });
      }

      // Add subtle hover effects to social icons
      document.querySelectorAll('footer a[aria-label]').forEach(icon => {
        icon.addEventListener('click', function(e) {
          e.preventDefault();
          console.log('Social media click:', this.getAttribute('aria-label'));
        });
      });

      console.log('📱 Footer initialized');
    });
  </script>
</body>
</html>